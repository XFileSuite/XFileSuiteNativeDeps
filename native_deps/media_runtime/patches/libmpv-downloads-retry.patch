diff --git a/cmd/downloads/main.go b/cmd/downloads/main.go
index 66c2916..65cde58 100644
--- a/cmd/downloads/main.go
+++ b/cmd/downloads/main.go
@@ -10,6 +10,7 @@ import (
 	"os"
 	"path"
 	"strings"
+	"time"
 
 	"github.com/media-kit/libmpv-darwin-build/pkg/lock"
 )
@@ -35,14 +36,23 @@ func main() {
 
 		log.Println(destPath)
 
-		err := download(dep.URL, tmpPath)
-		if err != nil {
-			log.Fatalf("%s: %s", destPath, err)
+		var downloadErr error
+		for attempt := 1; attempt <= 5; attempt++ {
+			_ = os.Remove(tmpPath)
+			downloadErr = download(dep.URL, tmpPath)
+			if downloadErr == nil {
+				downloadErr = check(tmpPath, dep.Sha256)
+			}
+			if downloadErr == nil {
+				break
+			}
+			log.Printf("%s: attempt %d/5 failed: %s", destPath, attempt, downloadErr)
+			if attempt < 5 {
+				time.Sleep(time.Duration(attempt) * 2 * time.Second)
+			}
 		}
-
-		err = check(tmpPath, dep.Sha256)
-		if err != nil {
-			log.Fatalf("%s: %s", destPath, err)
+		if downloadErr != nil {
+			log.Fatalf("%s: %s", destPath, downloadErr)
 		}
 
 		err = os.Rename(tmpPath, destPath)
@@ -84,7 +94,8 @@ func download(url, path string) error {
 		return fmt.Errorf("download: %w", err)
 	}
 
-	res, err := http.DefaultClient.Do(req)
+	client := &http.Client{Timeout: 3 * time.Minute}
+	res, err := client.Do(req)
 	if err != nil {
 		return fmt.Errorf("download: %w", err)
 	}
